import 'package:flutter/material.dart';
import '../widgets/checkBooking_widget.dart';

class CheckBookingByFacePage extends StatelessWidget {
  const CheckBookingByFacePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CheckBookingByFaceWidget(),

    );
  }
}
